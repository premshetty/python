
#inputs

name = input('name : ')
age = input('Age: ')
city = input('City: ')

print(f"Hi {name} your Age is {age} and living in {city}")

# arithmetic

a = int(input('value A: '))
b = int(input('value B: '))

print({
    "add": a + b,
    "subtract": a - b,
    "multiply": a * b,
    "divide": a / b,
    'floorDivision' : a//b,
    'exponent' : a ** b,
    
})